# <Slice Name> Slice Review

## Slice

Workstream: `<Workstream name>`
Slice plan: `intent/<area>/workstreams/<yyyy-mm-dd-topic>/<NN>-slice-plan-<topic>.md`
Implementation date: `<YYYY-MM-DD>`
Status: `<accepted | accepted with follow-ups | needs revision | blocked>`

## Implemented Scope

- <What was implemented>

The implementation stops before:

- <Future-gated or explicitly excluded work>

## Runtime Changes

Frontend:
- `<implementation-root>/...`: <summary>

Backend/API:
- `<implementation-root>/...`: <summary>

Data / persistence:
- `<implementation-root>/...`: <summary>

Tests:
- `<implementation-root>/...`: <summary>

Intent / roadmap:
- `intent/...`: <summary>

## Validation

Automated checks:
- `<command>` passed/failed: <result>

Manual or browser smoke:
- <Observed path and result>

Accessibility or usability checks:
- <Observed result>

## Repository Publication Check

New tracked files or folders:
- `<path>`: <public source | provider-specific source | generated artifact | private/local artifact | intent-only project memory>

Ignored or local-only files/folders reviewed:
- `<path or pattern>`: <reason>

Publication decision:
- <Tracked intentionally | moved local-only | added to ignore rules | parked for follow-up>

Provider-neutrality check:
- <No provider lock-in implied, or document why provider-specific files are tracked.>

Sensitive-data check:
- <No secrets, private emails, credentials, raw exports, generated provider output, or private operational notes included.>

## Agent Coverage

- Reused existing agent findings:
- Newly spawned agents and why reuse was insufficient:
- Agents closed after use:
- Consulted agents:
- Intentionally excluded specialists:
- Implementation re-consultation triggers:

## Review Notes

- <Reviewer acceptance note, residual risk, or important constraint>

Known limitations by design:

- <Limitation>

## Review Stop Condition

Review can stop when reviewers can confirm:

- <Acceptance condition>
- <Acceptance condition>

## Residual Risks

| Risk | Impact | Mitigation or follow-up |
| --- | --- | --- |
| <Risk> | <Impact> | <Mitigation> |

## Recommended Tracker Updates

Feature backlog:
- `<F-###>`: <status recommendation>

Bug ledger:
- `<BUG-###>`: <status recommendation>

Workstream tracker:
- `<Workstream>`: <next posture>

Active context:
- <Update needed or "No active-context update needed">

Compact feature artifacts:
- <Feature status map / slice ledger update needed, or "Not using compact mode">

## Recommended Next Slice Development Topic

<Recommend the next best slice development topic and why it should come next. Include whether to accept, revise, park, or escalate the current slice before starting that next topic.>
